# Restricted sudoers entry for the wg-registry service user.
# Only `wg set/show` on the configured interface is permitted; nothing else.
wg-registry ALL=(root) NOPASSWD: /usr/bin/wg set wg0 peer *, /usr/bin/wg show wg0 dump, /usr/bin/wg show wg0 allowed-ips
